This repo contains a SIMPLE CRUD operations using REST APIS 
APIS contains 
GET
POST
DELETE

To run this repo in your local provide a .env file that contains your DB Details 
HOST
PORT
PASSWORD
DATABASE

Then run the following commands
npm install
npm run dev
